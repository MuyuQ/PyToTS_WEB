---
title: Python ↔ TypeScript 手册
description: 快速查阅 Python 和 TypeScript 的语法对照
---

# Python ↔ TypeScript 手册

快速查阅两种语言的语法对照和差异说明。

## 使用方式

本手册按照以下结构组织：

1. **语法对照**：Python 和 TypeScript 的并排代码对比
2. **差异说明**：关键差异和常见陷阱
3. **最佳实践**：推荐的使用方式

## 快速导航

| 主题 | Python | TypeScript |
|------|--------|------------|
| 变量声明 | `x = 1` | `let x: number = 1` |
| 函数定义 | `def foo():` | `function foo(): void` |
| 类型注解 | `x: int` | `x: number` |
| 类定义 | `class Foo:` | `class Foo {}` |
| 异步函数 | `async def foo():` | `async function foo(): Promise<void>` |