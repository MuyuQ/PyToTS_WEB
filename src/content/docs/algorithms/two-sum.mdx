---
title: 两数之和 (Two Sum)
kind: algorithm
difficulty: easy
tags: ["array", "hash-table"]
time_complexity: "O(n)"
space_complexity: "O(n)"
description: 给定数组和目标值，找出两数之和等于目标值的索引
---

import DualCodeBlock from "../../../components/DualCodeBlock.astro";

## 问题描述

给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出和为目标值 `target` 的那两个整数，并返回它们的数组下标。

## 思路分析

使用哈希表存储已遍历的数字及其索引，对于每个数字，检查 `target - num` 是否在哈希表中。

## 复杂度分析

- **时间复杂度**：O(n)，只需遍历一次数组
- **空间复杂度**：O(n)，哈希表最多存储 n 个元素

## 双语实现

<DualCodeBlock
  title="核心实现"
  python={`# 两数之和 - Python 实现
# 时间复杂度: O(n), 空间复杂度: O(n)
# 核心思路: 用哈希表记录已遍历元素的值和索引
def two_sum(nums: list[int], target: int) -> list[int]:
    # 哈希表存储值到索引的映射
    # 键: 数组元素值, 值: 该元素在数组中的索引
    seen = {}
    
    # enumerate 同时获取索引和值，避免二次查找
    for i, num in enumerate(nums):
        # 计算需要的补数: 如果补数已在表中，说明找到答案
        complement = target - num
        
        # 检查补数是否已在哈希表中
        # 如果在，说明之前遍历过该补数，返回两个索引
        if complement in seen:
            return [seen[complement], i]
        
        # 将当前值和索引存入哈希表，供后续元素查找
        seen[num] = i
    
    # 题目保证有解，这里理论上不会执行到
    return []

# 测试用例

print(two_sum([2, 7, 11, 15], 9)) # [0, 1] 因为 nums[0]+nums[1]=2+7=9
print(two_sum([3, 2, 4], 6)) # [1, 2] 因为 nums[1]+nums[2]=2+4=6`}
  typescript={`// 两数之和 - TypeScript 实现
// 时间复杂度: O(n), 空间复杂度: O(n)
// 核心思路: 用 Map 记录已遍历元素的值和索引
function twoSum(nums: number[], target: number): number[] {
// 使用 Map 存储值到索引的映射
// 键: 数组元素值, 值: 该元素在数组中的索引
const seen = new Map<number, number>();

    // 遍历数组，用传统 for 循环获取索引
    for (let i = 0; i < nums.length; i++) {
        const num = nums[i];
        // 计算需要的补数: 如果补数已在 Map 中，说明找到答案
        const complement = target - num;

        // 检查补数是否已在 Map 中
        // has() 方法检查键是否存在，O(1) 时间复杂度
        if (seen.has(complement)) {
            // get() 返回补数的索引，! 断言非空（因为 has 已确认存在）
            return [seen.get(complement)!, i];
        }

        // 将当前值和索引存入 Map，供后续元素查找
        seen.set(num, i);
    }

    // 题目保证有解，这里理论上不会执行到
    return [];

}

// 测试用例
console.log(twoSum([2, 7, 11, 15], 9)); // [0, 1] 因为 nums[0]+nums[1]=2+7=9
console.log(twoSum([3, 2, 4], 6)); // [1, 2] 因为 nums[1]+nums[2]=2+4=6`}
/>

## 面试变体

1. **返回所有组合**：如果有多个解，返回所有不重复的组合
2. **有序数组**：如果数组已排序，可以使用双指针法
3. **三数之和**：扩展到三个数之和等于目标值

## 常见错误

1. 忘记检查 `complement` 是否在哈希表中就返回
2. 使用两层循环导致 O(n²) 时间复杂度
3. 重复使用同一元素（题目通常不允许）
