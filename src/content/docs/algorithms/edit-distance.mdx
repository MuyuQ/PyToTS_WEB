---
title: 编辑距离 (Edit Distance)
kind: algorithm
difficulty: medium
tags: ["dynamic-programming", "string"]
time_complexity: "O(m * n)"
space_complexity: "O(n)"
description: 计算将一个字符串转换成另一个字符串的最少操作数
---

import DualCodeBlock from "../../../components/DualCodeBlock.astro";

## 问题描述

给你两个单词 `word1` 和 `word2`，请返回将 `word1` 转换成 `word2` 所使用的最少操作数。

你可以对一个单词进行如下三种操作：

- 插入一个字符
- 删除一个字符
- 替换一个字符

## 思路分析

动态规划：

- `dp[i][j]`：`word1[0..i-1]` 转换成 `word2[0..j-1]` 的最少操作数
- 状态转移：
  - 如果 `word1[i-1] == word2[j-1]`：`dp[i][j] = dp[i-1][j-1]`
  - 否则：`dp[i][j] = min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]) + 1`
    - `dp[i-1][j]` + 1：删除 `word1[i-1]`
    - `dp[i][j-1]` + 1：插入 `word2[j-1]`
    - `dp[i-1][j-1]` + 1：替换 `word1[i-1]` 为 `word2[j-1]`

## 复杂度分析

- **时间复杂度**：O(m \* n)，m 和 n 是两个单词长度
- **空间复杂度**：O(n)，可以优化为一维数组

## 双语实现

<DualCodeBlock
  title="核心实现"
  python={`# 编辑距离 - 动态规划 (一维空间优化)
# 算法: Edit Distance (Levenshtein Distance)
# 时间复杂度: O(m * n), m和n是两个单词长度
# 空间复杂度: O(n), 优化为一维数组

def min_distance(word1: str, word2: str) -> int: # 获取两个单词的长度
m, n = len(word1), len(word2)

    # 一维DP数组优化：dp[j] 表示 word1[0..i-1] 转换成 word2[0..j-1] 的最少操作数
    # 初始状态：word1 为空字符串时，转换成 word2[0..j-1] 需要 j 次插入操作
    dp = list(range(n + 1))  # [0, 1, 2, ..., n]

    # 外层循环：遍历 word1 的每个字符（索引1到m）
    for i in range(1, m + 1):
        # prev 保存 dp[i-1][j-1] 的值（左上角）
        # 在更新 dp[j] 之前，dp[0] 就是 dp[i-1][0]
        prev = dp[0]  # 保存上一行左上角的值

        # 更新第一列：word1[0..i-1] 转换成空字符串需要 i 次删除操作
        dp[0] = i  # 第一列的值

        # 内层循环：遍历 word2 的每个字符（索引1到n）
        for j in range(1, n + 1):
            # temp 保存当前 dp[j] 的值，即 dp[i-1][j]（正上方）
            # 这个值会在下一轮循环中作为左上角的值
            temp = dp[j]  # 保存上一行正上方的值

            # 比较当前字符是否相同
            if word1[i - 1] == word2[j - 1]:
                # 字符相同：不需要操作，编辑距离等于左上角的值
                dp[j] = prev
            else:
                # 字符不同：取三种操作的最小值 + 1
                # dp[j]: 删除 word1[i-1]，编辑距离 = 上方值 + 1
                # dp[j-1]: 插入 word2[j-1]，编辑距离 = 左方值 + 1
                # prev: 替换 word1[i-1] 为 word2[j-1]，编辑距离 = 左上角值 + 1
                dp[j] = min(dp[j], dp[j - 1], prev) + 1

            # 更新 prev 为下一轮循环的左上角值
            prev = temp

    # 返回右下角的值，即完整两个单词的编辑距离
    return dp[n]

# 测试用例

print(min_distance("horse", "ros")) # 3 (horse->rorse->rose->ros)
print(min_distance("intention", "execution")) # 5
print(min_distance("", "")) # 0 (两个空字符串)
print(min_distance("a", "")) # 1 (删除'a')`}
  typescript={`// 编辑距离 - 动态规划 (一维空间优化)
// 算法: Edit Distance (Levenshtein Distance)
// 时间复杂度: O(m \* n), m和n是两个单词长度
// 空间复杂度: O(n), 优化为一维数组

function minDistance(word1: string, word2: string): number {
// 获取两个单词的长度
const m = word1.length;
const n = word2.length;

    // 一维DP数组优化：dp[j] 表示 word1[0..i-1] 转换成 word2[0..j-1] 的最少操作数
    // 初始状态：word1 为空字符串时，转换成 word2[0..j-1] 需要 j 次插入操作
    const dp: number[] = Array.from({ length: n + 1 }, (_, i) => i);  // [0, 1, 2, ..., n]

    // 外层循环：遍历 word1 的每个字符（索引1到m）
    for (let i = 1; i <= m; i++) {
        // prev 保存 dp[i-1][j-1] 的值（左上角）
        // 在更新 dp[j] 之前，dp[0] 就是 dp[i-1][0]
        let prev = dp[0];  // 保存上一行左上角的值

        // 更新第一列：word1[0..i-1] 转换成空字符串需要 i 次删除操作
        dp[0] = i;  // 第一列的值

        // 内层循环：遍历 word2 的每个字符（索引1到n）
        for (let j = 1; j <= n; j++) {
            // temp 保存当前 dp[j] 的值，即 dp[i-1][j]（正上方）
            // 这个值会在下一轮循环中作为左上角的值
            const temp = dp[j];  // 保存上一行正上方的值

            // 比较当前字符是否相同
            if (word1[i - 1] === word2[j - 1]) {
                // 字符相同：不需要操作，编辑距离等于左上角的值
                dp[j] = prev;
            } else {
                // 字符不同：取三种操作的最小值 + 1
                // dp[j]: 删除 word1[i-1]，编辑距离 = 上方值 + 1
                // dp[j-1]: 插入 word2[j-1]，编辑距离 = 左方值 + 1
                // prev: 替换 word1[i-1] 为 word2[j-1]，编辑距离 = 左上角值 + 1
                dp[j] = Math.min(dp[j], dp[j - 1], prev) + 1;
            }

            // 更新 prev 为下一轮循环的左上角值
            prev = temp;
        }
    }

    // 返回右下角的值，即完整两个单词的编辑距离
    return dp[n];

}

// 测试用例
console.log(minDistance("horse", "ros")); // 3
console.log(minDistance("intention", "execution")); // 5
console.log(minDistance("", "")); // 0
console.log(minDistance("a", "")); // 1`}
/>

## 面试变体

1. **删除距离**：只能删除
2. **一次编辑**：判断是否可以通过一次编辑转换
3. **正则表达式匹配**：通配符匹配问题

## 常见错误

1. 一维优化时变量更新顺序错误
2. 初始化边界条件错误
3. 混淆行和列的索引
